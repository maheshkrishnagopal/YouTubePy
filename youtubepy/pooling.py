"""

"""
from multiprocessing import Pool


def init_driver():
    """

    :return:
    """


if __name__ == "__main__":
    P = Pool(cnt)
    P.map(self.init_driver, zip(drivers, url_passing))